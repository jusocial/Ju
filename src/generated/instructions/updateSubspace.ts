/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as beet from '@metaplex-foundation/beet'
import * as web3 from '@solana/web3.js'
import { SubspaceData, subspaceDataBeet } from '../types/SubspaceData'

/**
 * @category Instructions
 * @category UpdateSubspace
 * @category generated
 */
export type UpdateSubspaceInstructionArgs = {
  data: SubspaceData
}
/**
 * @category Instructions
 * @category UpdateSubspace
 * @category generated
 */
export const updateSubspaceStruct = new beet.FixableBeetArgsStruct<
  UpdateSubspaceInstructionArgs & {
    instructionDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['instructionDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['data', subspaceDataBeet],
  ],
  'UpdateSubspaceInstructionArgs'
)
/**
 * Accounts required by the _updateSubspace_ instruction
 *
 * @property [] app
 * @property [] profile
 * @property [_writable_] subspace
 * @property [_writable_] currentAliasPda (optional)
 * @property [_writable_] newAliasPda (optional)
 * @property [] connectingProcessorPda (optional)
 * @property [] publishingProcessorPda (optional)
 * @property [] collectingProcessorPda (optional)
 * @property [] referencingProcessorPda (optional)
 * @property [_writable_, **signer**] authority
 * @category Instructions
 * @category UpdateSubspace
 * @category generated
 */
export type UpdateSubspaceInstructionAccounts = {
  app: web3.PublicKey
  profile: web3.PublicKey
  subspace: web3.PublicKey
  currentAliasPda?: web3.PublicKey
  newAliasPda?: web3.PublicKey
  connectingProcessorPda?: web3.PublicKey
  publishingProcessorPda?: web3.PublicKey
  collectingProcessorPda?: web3.PublicKey
  referencingProcessorPda?: web3.PublicKey
  authority: web3.PublicKey
  systemProgram?: web3.PublicKey
  anchorRemainingAccounts?: web3.AccountMeta[]
}

export const updateSubspaceInstructionDiscriminator = [
  68, 66, 37, 238, 181, 13, 57, 20,
]

/**
 * Creates a _UpdateSubspace_ instruction.
 *
 * Optional accounts that are not provided will be omitted from the accounts
 * array passed with the instruction.
 * An optional account that is set cannot follow an optional account that is unset.
 * Otherwise an Error is raised.
 *
 * @param accounts that will be accessed while the instruction is processed
 * @param args to provide as instruction data to the program
 *
 * @category Instructions
 * @category UpdateSubspace
 * @category generated
 */
export function createUpdateSubspaceInstruction(
  accounts: UpdateSubspaceInstructionAccounts,
  args: UpdateSubspaceInstructionArgs,
  programId = new web3.PublicKey('964vWgVEK9X8ZwZB2HyshFVmHUWbcYpRTnVYz2o3F2Xq')
) {
  const [data] = updateSubspaceStruct.serialize({
    instructionDiscriminator: updateSubspaceInstructionDiscriminator,
    ...args,
  })
  const keys: web3.AccountMeta[] = [
    {
      pubkey: accounts.app,
      isWritable: false,
      isSigner: false,
    },
    {
      pubkey: accounts.profile,
      isWritable: false,
      isSigner: false,
    },
    {
      pubkey: accounts.subspace,
      isWritable: true,
      isSigner: false,
    },
  ]

  if (accounts.currentAliasPda != null) {
    keys.push({
      pubkey: accounts.currentAliasPda,
      isWritable: true,
      isSigner: false,
    })
  }
  if (accounts.newAliasPda != null) {
    if (accounts.currentAliasPda == null) {
      throw new Error(
        "When providing 'newAliasPda' then 'accounts.currentAliasPda' need(s) to be provided as well."
      )
    }
    keys.push({
      pubkey: accounts.newAliasPda,
      isWritable: true,
      isSigner: false,
    })
  }
  if (accounts.connectingProcessorPda != null) {
    if (accounts.currentAliasPda == null || accounts.newAliasPda == null) {
      throw new Error(
        "When providing 'connectingProcessorPda' then 'accounts.currentAliasPda', 'accounts.newAliasPda' need(s) to be provided as well."
      )
    }
    keys.push({
      pubkey: accounts.connectingProcessorPda,
      isWritable: false,
      isSigner: false,
    })
  }
  if (accounts.publishingProcessorPda != null) {
    if (
      accounts.currentAliasPda == null ||
      accounts.newAliasPda == null ||
      accounts.connectingProcessorPda == null
    ) {
      throw new Error(
        "When providing 'publishingProcessorPda' then 'accounts.currentAliasPda', 'accounts.newAliasPda', 'accounts.connectingProcessorPda' need(s) to be provided as well."
      )
    }
    keys.push({
      pubkey: accounts.publishingProcessorPda,
      isWritable: false,
      isSigner: false,
    })
  }
  if (accounts.collectingProcessorPda != null) {
    if (
      accounts.currentAliasPda == null ||
      accounts.newAliasPda == null ||
      accounts.connectingProcessorPda == null ||
      accounts.publishingProcessorPda == null
    ) {
      throw new Error(
        "When providing 'collectingProcessorPda' then 'accounts.currentAliasPda', 'accounts.newAliasPda', 'accounts.connectingProcessorPda', 'accounts.publishingProcessorPda' need(s) to be provided as well."
      )
    }
    keys.push({
      pubkey: accounts.collectingProcessorPda,
      isWritable: false,
      isSigner: false,
    })
  }
  if (accounts.referencingProcessorPda != null) {
    if (
      accounts.currentAliasPda == null ||
      accounts.newAliasPda == null ||
      accounts.connectingProcessorPda == null ||
      accounts.publishingProcessorPda == null ||
      accounts.collectingProcessorPda == null
    ) {
      throw new Error(
        "When providing 'referencingProcessorPda' then 'accounts.currentAliasPda', 'accounts.newAliasPda', 'accounts.connectingProcessorPda', 'accounts.publishingProcessorPda', 'accounts.collectingProcessorPda' need(s) to be provided as well."
      )
    }
    keys.push({
      pubkey: accounts.referencingProcessorPda,
      isWritable: false,
      isSigner: false,
    })
  }
  keys.push({
    pubkey: accounts.authority,
    isWritable: true,
    isSigner: true,
  })
  keys.push({
    pubkey: accounts.systemProgram ?? web3.SystemProgram.programId,
    isWritable: false,
    isSigner: false,
  })

  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc)
    }
  }

  const ix = new web3.TransactionInstruction({
    programId,
    keys,
    data,
  })
  return ix
}
