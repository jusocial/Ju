/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as beet from '@metaplex-foundation/beet';
import * as web3 from '@solana/web3.js';
import { SubspaceData, subspaceDataBeet } from '../types/SubspaceData';

/**
 * @category Instructions
 * @category CreateSubspace
 * @category generated
 */
export type CreateSubspaceInstructionArgs = {
  uuid: string;
  data: SubspaceData;
};
/**
 * @category Instructions
 * @category CreateSubspace
 * @category generated
 */
export const createSubspaceStruct = new beet.FixableBeetArgsStruct<
  CreateSubspaceInstructionArgs & {
    instructionDiscriminator: number[] /* size: 8 */;
  }
>(
  [
    ['instructionDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['uuid', beet.utf8String],
    ['data', subspaceDataBeet],
  ],
  'CreateSubspaceInstructionArgs',
);
/**
 * Accounts required by the _createSubspace_ instruction
 *
 * @property [] app
 * @property [] creatorProfile
 * @property [_writable_] subspace
 * @property [_writable_] aliasPda (optional)
 * @property [] connectingProcessorPda (optional)
 * @property [] publishingProcessorPda (optional)
 * @property [] collectingProcessorPda (optional)
 * @property [] referencingProcessorPda (optional)
 * @property [_writable_, **signer**] authority
 * @category Instructions
 * @category CreateSubspace
 * @category generated
 */
export type CreateSubspaceInstructionAccounts = {
  app: web3.PublicKey;
  creatorProfile: web3.PublicKey;
  subspace: web3.PublicKey;
  aliasPda?: web3.PublicKey;
  connectingProcessorPda?: web3.PublicKey;
  publishingProcessorPda?: web3.PublicKey;
  collectingProcessorPda?: web3.PublicKey;
  referencingProcessorPda?: web3.PublicKey;
  authority: web3.PublicKey;
  systemProgram?: web3.PublicKey;
};

export const createSubspaceInstructionDiscriminator = [209, 236, 139, 82, 188, 121, 89, 27];

/**
 * Creates a _CreateSubspace_ instruction.
 *
 * Optional accounts that are not provided will be omitted from the accounts
 * array passed with the instruction.
 * An optional account that is set cannot follow an optional account that is unset.
 * Otherwise an Error is raised.
 *
 * @param accounts that will be accessed while the instruction is processed
 * @param args to provide as instruction data to the program
 *
 * @category Instructions
 * @category CreateSubspace
 * @category generated
 */
export function createCreateSubspaceInstruction(
  accounts: CreateSubspaceInstructionAccounts,
  args: CreateSubspaceInstructionArgs,
  programId = new web3.PublicKey('964vWgVEK9X8ZwZB2HyshFVmHUWbcYpRTnVYz2o3F2Xq'),
) {
  const [data] = createSubspaceStruct.serialize({
    instructionDiscriminator: createSubspaceInstructionDiscriminator,
    ...args,
  });
  const keys: web3.AccountMeta[] = [
    {
      pubkey: accounts.app,
      isWritable: false,
      isSigner: false,
    },
    {
      pubkey: accounts.creatorProfile,
      isWritable: false,
      isSigner: false,
    },
    {
      pubkey: accounts.subspace,
      isWritable: true,
      isSigner: false,
    },
  ];

  if (accounts.aliasPda != null) {
    keys.push({
      pubkey: accounts.aliasPda,
      isWritable: true,
      isSigner: false,
    });
  }
  if (accounts.connectingProcessorPda != null) {
    if (accounts.aliasPda == null) {
      throw new Error(
        "When providing 'connectingProcessorPda' then 'accounts.aliasPda' need(s) to be provided as well.",
      );
    }
    keys.push({
      pubkey: accounts.connectingProcessorPda,
      isWritable: false,
      isSigner: false,
    });
  }
  if (accounts.publishingProcessorPda != null) {
    if (accounts.aliasPda == null || accounts.connectingProcessorPda == null) {
      throw new Error(
        "When providing 'publishingProcessorPda' then 'accounts.aliasPda', 'accounts.connectingProcessorPda' need(s) to be provided as well.",
      );
    }
    keys.push({
      pubkey: accounts.publishingProcessorPda,
      isWritable: false,
      isSigner: false,
    });
  }
  if (accounts.collectingProcessorPda != null) {
    if (
      accounts.aliasPda == null ||
      accounts.connectingProcessorPda == null ||
      accounts.publishingProcessorPda == null
    ) {
      throw new Error(
        "When providing 'collectingProcessorPda' then 'accounts.aliasPda', 'accounts.connectingProcessorPda', 'accounts.publishingProcessorPda' need(s) to be provided as well.",
      );
    }
    keys.push({
      pubkey: accounts.collectingProcessorPda,
      isWritable: false,
      isSigner: false,
    });
  }
  if (accounts.referencingProcessorPda != null) {
    if (
      accounts.aliasPda == null ||
      accounts.connectingProcessorPda == null ||
      accounts.publishingProcessorPda == null ||
      accounts.collectingProcessorPda == null
    ) {
      throw new Error(
        "When providing 'referencingProcessorPda' then 'accounts.aliasPda', 'accounts.connectingProcessorPda', 'accounts.publishingProcessorPda', 'accounts.collectingProcessorPda' need(s) to be provided as well.",
      );
    }
    keys.push({
      pubkey: accounts.referencingProcessorPda,
      isWritable: false,
      isSigner: false,
    });
  }
  keys.push({
    pubkey: accounts.authority,
    isWritable: true,
    isSigner: true,
  });
  keys.push({
    pubkey: accounts.systemProgram ?? web3.SystemProgram.programId,
    isWritable: false,
    isSigner: false,
  });

  const ix = new web3.TransactionInstruction({
    programId,
    keys,
    data,
  });
  return ix;
}
