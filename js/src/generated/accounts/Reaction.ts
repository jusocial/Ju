/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js';
import * as beet from '@metaplex-foundation/beet';
import * as beetSolana from '@metaplex-foundation/beet-solana';
import { ReactionTargetType, reactionTargetTypeBeet } from '../types/ReactionTargetType';
import { ReactionType, reactionTypeBeet } from '../types/ReactionType';

/**
 * Arguments used to create {@link Reaction}
 * @category Accounts
 * @category generated
 */
export type ReactionArgs = {
  app: web3.PublicKey;
  authority: web3.PublicKey;
  targetType: ReactionTargetType;
  target: web3.PublicKey;
  initializer: web3.PublicKey;
  reactionType: ReactionType;
  createdAt: beet.bignum;
};

export const reactionDiscriminator = [226, 61, 100, 191, 223, 221, 142, 139];
/**
 * Holds the data for the {@link Reaction} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class Reaction implements ReactionArgs {
  private constructor(
    readonly app: web3.PublicKey,
    readonly authority: web3.PublicKey,
    readonly targetType: ReactionTargetType,
    readonly target: web3.PublicKey,
    readonly initializer: web3.PublicKey,
    readonly reactionType: ReactionType,
    readonly createdAt: beet.bignum,
  ) {}

  /**
   * Creates a {@link Reaction} instance from the provided args.
   */
  static fromArgs(args: ReactionArgs) {
    return new Reaction(
      args.app,
      args.authority,
      args.targetType,
      args.target,
      args.initializer,
      args.reactionType,
      args.createdAt,
    );
  }

  /**
   * Deserializes the {@link Reaction} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo: web3.AccountInfo<Buffer>, offset = 0): [Reaction, number] {
    return Reaction.deserialize(accountInfo.data, offset);
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Reaction} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig,
  ): Promise<Reaction> {
    const accountInfo = await connection.getAccountInfo(address, commitmentOrConfig);
    if (accountInfo == null) {
      throw new Error(`Unable to find Reaction account at ${address}`);
    }
    return Reaction.fromAccountInfo(accountInfo, 0)[0];
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey('964vWgVEK9X8ZwZB2HyshFVmHUWbcYpRTnVYz2o3F2Xq'),
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, reactionBeet);
  }

  /**
   * Deserializes the {@link Reaction} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [Reaction, number] {
    return reactionBeet.deserialize(buf, offset);
  }

  /**
   * Serializes the {@link Reaction} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return reactionBeet.serialize({
      accountDiscriminator: reactionDiscriminator,
      ...this,
    });
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Reaction}
   */
  static get byteSize() {
    return reactionBeet.byteSize;
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Reaction} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment,
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(Reaction.byteSize, commitment);
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link Reaction} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === Reaction.byteSize;
  }

  /**
   * Returns a readable version of {@link Reaction} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      app: this.app.toBase58(),
      authority: this.authority.toBase58(),
      targetType: 'ReactionTargetType.' + ReactionTargetType[this.targetType],
      target: this.target.toBase58(),
      initializer: this.initializer.toBase58(),
      reactionType: 'ReactionType.' + ReactionType[this.reactionType],
      createdAt: (() => {
        const x = <{ toNumber: () => number }>this.createdAt;
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
    };
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const reactionBeet = new beet.BeetStruct<
  Reaction,
  ReactionArgs & {
    accountDiscriminator: number[] /* size: 8 */;
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['app', beetSolana.publicKey],
    ['authority', beetSolana.publicKey],
    ['targetType', reactionTargetTypeBeet],
    ['target', beetSolana.publicKey],
    ['initializer', beetSolana.publicKey],
    ['reactionType', reactionTypeBeet],
    ['createdAt', beet.i64],
  ],
  Reaction.fromArgs,
  'Reaction',
);
