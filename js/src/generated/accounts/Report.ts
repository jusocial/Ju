/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js';
import * as beet from '@metaplex-foundation/beet';
import * as beetSolana from '@metaplex-foundation/beet-solana';
import { ReportTargetType, reportTargetTypeBeet } from '../types/ReportTargetType';
import { ReportType, reportTypeBeet } from '../types/ReportType';

/**
 * Arguments used to create {@link Report}
 * @category Accounts
 * @category generated
 */
export type ReportArgs = {
  app: web3.PublicKey;
  authority: web3.PublicKey;
  targetType: ReportTargetType;
  target: web3.PublicKey;
  initializer: web3.PublicKey;
  reportType: ReportType;
  createdAt: beet.bignum;
  creationWeek: beet.bignum;
  creation3Day: beet.bignum;
  creationDay: beet.bignum;
  notification: beet.COption<string>;
};

export const reportDiscriminator = [232, 246, 229, 227, 242, 105, 190, 2];
/**
 * Holds the data for the {@link Report} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class Report implements ReportArgs {
  private constructor(
    readonly app: web3.PublicKey,
    readonly authority: web3.PublicKey,
    readonly targetType: ReportTargetType,
    readonly target: web3.PublicKey,
    readonly initializer: web3.PublicKey,
    readonly reportType: ReportType,
    readonly createdAt: beet.bignum,
    readonly creationWeek: beet.bignum,
    readonly creation3Day: beet.bignum,
    readonly creationDay: beet.bignum,
    readonly notification: beet.COption<string>,
  ) {}

  /**
   * Creates a {@link Report} instance from the provided args.
   */
  static fromArgs(args: ReportArgs) {
    return new Report(
      args.app,
      args.authority,
      args.targetType,
      args.target,
      args.initializer,
      args.reportType,
      args.createdAt,
      args.creationWeek,
      args.creation3Day,
      args.creationDay,
      args.notification,
    );
  }

  /**
   * Deserializes the {@link Report} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo: web3.AccountInfo<Buffer>, offset = 0): [Report, number] {
    return Report.deserialize(accountInfo.data, offset);
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Report} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig,
  ): Promise<Report> {
    const accountInfo = await connection.getAccountInfo(address, commitmentOrConfig);
    if (accountInfo == null) {
      throw new Error(`Unable to find Report account at ${address}`);
    }
    return Report.fromAccountInfo(accountInfo, 0)[0];
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey('964vWgVEK9X8ZwZB2HyshFVmHUWbcYpRTnVYz2o3F2Xq'),
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, reportBeet);
  }

  /**
   * Deserializes the {@link Report} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [Report, number] {
    return reportBeet.deserialize(buf, offset);
  }

  /**
   * Serializes the {@link Report} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return reportBeet.serialize({
      accountDiscriminator: reportDiscriminator,
      ...this,
    });
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Report} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args: ReportArgs) {
    const instance = Report.fromArgs(args);
    return reportBeet.toFixedFromValue({
      accountDiscriminator: reportDiscriminator,
      ...instance,
    }).byteSize;
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Report} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    args: ReportArgs,
    connection: web3.Connection,
    commitment?: web3.Commitment,
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(Report.byteSize(args), commitment);
  }

  /**
   * Returns a readable version of {@link Report} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      app: this.app.toBase58(),
      authority: this.authority.toBase58(),
      targetType: 'ReportTargetType.' + ReportTargetType[this.targetType],
      target: this.target.toBase58(),
      initializer: this.initializer.toBase58(),
      reportType: 'ReportType.' + ReportType[this.reportType],
      createdAt: (() => {
        const x = <{ toNumber: () => number }>this.createdAt;
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      creationWeek: (() => {
        const x = <{ toNumber: () => number }>this.creationWeek;
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      creation3Day: (() => {
        const x = <{ toNumber: () => number }>this.creation3Day;
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      creationDay: (() => {
        const x = <{ toNumber: () => number }>this.creationDay;
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
      notification: this.notification,
    };
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const reportBeet = new beet.FixableBeetStruct<
  Report,
  ReportArgs & {
    accountDiscriminator: number[] /* size: 8 */;
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['app', beetSolana.publicKey],
    ['authority', beetSolana.publicKey],
    ['targetType', reportTargetTypeBeet],
    ['target', beetSolana.publicKey],
    ['initializer', beetSolana.publicKey],
    ['reportType', reportTypeBeet],
    ['createdAt', beet.i64],
    ['creationWeek', beet.i64],
    ['creation3Day', beet.i64],
    ['creationDay', beet.i64],
    ['notification', beet.coption(beet.utf8String)],
  ],
  Report.fromArgs,
  'Report',
);
